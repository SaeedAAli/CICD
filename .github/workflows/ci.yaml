name: Coderco CICD Workflow
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -name : Trying pipeline again
      uses: actions/checkout@v2
      -name: We are testing this workflow
      run: "Jackpot"
